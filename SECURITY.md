# Security Policy

## Supported versions

屿阅尚未发布首个公开版本。目前只接受针对默认分支最新代码的安全报告；首个 Release 后，本文件会列出受支持版本。

## 私密报告漏洞

请不要在公开 Issue、Discussion、PR 或社交媒体中披露尚未修复的漏洞细节。

仓库公开后，维护者必须在首个 Release 前启用 GitHub Private Vulnerability Reporting。启用后，请在仓库的 **Security → Advisories → Report a vulnerability** 中提交私密报告。

如果该入口尚不可用，请只创建一个不包含技术细节的公开 Issue，询问维护者当前的私密安全联系方式；等待私密渠道建立后再发送复现信息。

报告建议包含：

- 受影响版本或 commit。
- 可复现步骤和最小样例文档。
- 实际影响，以及是否涉及文件读取、网络请求、脚本执行或资源耗尽。
- 已验证的缓解方式（如有）。

## 响应原则

- 维护者应先确认收到报告，再私下复现、评估、修复和协调披露。
- 修复发布前，不要求报告者公开漏洞。
- 有效报告将在征得报告者同意后获得致谢。
- 发布后的安全问题优先通过 GitHub Repository Security Advisory 协作和披露。

## 核心安全预期

- 本地文档不上传。
- 原始 HTML 不执行。
- 远程图片默认阻断，授权不跨标签或重启持久化。
- 文档不能读取其根目录之外的本机资源。
- Mermaid/SVG 不得绕过网络、脚本、文件或容量边界。
- 外部链接只能在明确用户点击后由最小权限系统调用打开。

以上默认值构成本项目的公开安全边界。
